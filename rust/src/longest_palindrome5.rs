use crate::solution::Solution;

impl Solution {
    pub fn longest_palindrome(s: String) -> String {
        let len = s.len();
        let mut grid: Vec<Vec<bool>> = vec![vec![false; len]; len];
        for l in (2..len) {

        }
        String::new()
    }
}